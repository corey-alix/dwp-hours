export const styles = `<style>
  :host {
    display: block;
    padding: var(--space-md, 16px);
  }

  .pto-summary {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-md, 16px);
  }
</style>`;
