/**
 * Interaction constants — shared timing/threshold values used by
 * components for consistent gesture detection.
 */

/** Minimum hold duration (ms) to trigger a long-press action. */
export const LONG_PRESS_MS = 500;

/** Movement threshold (px) beyond which a long-press is cancelled. */
export const LONG_PRESS_MOVE_THRESHOLD = 10;
